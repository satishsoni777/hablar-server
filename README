Open an SSH client.

Locate your private key file. The key used to launch this instance is fit-server-key.pem

Run this command, if necessary, to ensure your key is not publicly viewable.
 chmod 400 fit-server-key.pem

Connect to your instance using its Public DNS:
 ec2-13-232-51-238.ap-south-1.compute.amazonaws.com

Example:

fit-server : ssh -i "fit-server-key.pem" ubuntu@ec2-13-127-178-129.ap-south-1.compute.amazonaws.com

 pm2 start server.js


- 1. Random call
- 2. Chat history
- 3. profile details
- 4. Login [gmail facebook, otp]

Unit Test
step 1  npm install mocha chai supertest --save-dev
step 2 npm install chai --save-dev
run npx mocha 